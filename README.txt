Tasks done in Nodejs and Typescript!
use "tsc" or "node run tsc" in order to compile Typescript files,
then use "npm run <script>" in order to run a script
scripts include:
    yearsToDays,
    fibonacci,
    passwordValidator,
    binarySearch,
    textSearch,
    timer,
    promise,
    counter,
    shapes,
    tests,